{
  "name": "{{ name }}",
  "version": "1.0.0",
  "description": "{{{ description }}}",
  "author": "{{{ author }}}",
  "license": "ISC",
  "repository": "https://github.com/{{ owner }}/{{ repo }}.git",
  "scripts": {
    "start": "probot run ./index.js",
    "test": "mocha && standard"
  },
  "dependencies": {
    "probot": "^2.0.0"
  },
  "devDependencies": {
    "jest": "^21.2.1",
    "localtunnel": "^1.8.2",
    "standard": "^10.0.3"
  },
  "engines": {
    "node": ">= 7.7.0",
    "npm": ">= 4.0.0"
  },
  "standard": {
    "env": [
      "jest"
    ]
  }
}
